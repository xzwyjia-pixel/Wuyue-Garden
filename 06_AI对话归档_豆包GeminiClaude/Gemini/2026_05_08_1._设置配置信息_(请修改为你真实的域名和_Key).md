{"content":"---\ntitle: 这样正确吗？\nsource: gemini\ndate: 2026-05-08\ncategory: 系统运维\nsubcategory: PowerShell\nold_category: 系统运维\ntags: [gemini, 系统运维, PowerShell]\n---\n\n## 这样正确吗？\n\n## 这样正确吗？\n\n从你提供的截图来看，脚本在执行过程中遇到了两个主要问题，导致配置没有真正生效。我们需要修正脚本的逻辑来适配你的环境。\n报错原因分析\n\nSetValueInvocationException (属性不存在)：\n由于你的 settings.json 原本可能没有 cline.anthropicBaseUrl 这些字段，PowerShell 的 PSCustomObject 无法直接通过点号赋值。我们需要改用“强行添加”的方式。\nReceived an unexpected EOF：\n这说明 Invoke-RestMethod 发起的测试请求虽然到了中转站，但因为 SSL 握手或代理断开（EOF 意为文件结束符/连接过早关闭），导致没能拿到数据。\n\n修正后的自动化脚本\n请直接在 PowerShell 中复制并运行下面这段修正后的代码。它解决了对象属性不存在的问题，并强化了请求的稳定性：\n# 1. 设置配置信息 (请修改为你真实的域名和 Key)\n$myBaseURL = \"https://你的中转域名.com/v1\"\n$apiKey = \"你的_API_KEY\"\n\n# 2. 自动化修改 VS Code 配置文件\n$settingsPath = \"$env:APPDATA\\Code\\User\\settings.json\"\n\nif (Test-Path $settingsPath) {\n    # 读取并解析 JSON\n    $jsonText = Get-Content $settingsPath -Raw -Encoding utf8\n    $settings = $jsonText | ConvertFrom-Json\n    \n    # 使用强行添加成员的方式，解决属性不存在的报错\n    $newSettings = @{\n        \"cline.anthropicBaseUrl\" = $myBaseURL\n        \"anthropic.baseUrl\"      = $myBaseURL\n    }\n    \n    foreach ($key in $newSettings.Keys) {\n        if ($settings.PSObject.Properties[$key]) {\n            $settings.$key = $newSettings[$key]\n        } else {\n            $settings | Add-Member -MemberType NoteProperty -Name $key -Value $newSettings[$key]\n        }\n    }\n    \n    # 写回文件\n    $settings | ConvertTo-Json -Depth 100 | Out-File $settingsPath -Encoding utf8\n    Write-Host \"✅ VS Code 配置已成功强制更新。\" -ForegroundColor Green\n}\n\n# 3. 解决测试请求的 EOF 错误 (设置安全协议)\n[Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12 -bor [Net.SecurityProtocolType]::Tls13\n\n# 4. 发起最终验证\n$testPayload = @{\n    model = \"claude-3-5-sonnet-20240620\"\n    max_tokens = 10\n    messages = @(@{ role = \"user\"; content = \"hi\" })\n} | ConvertTo-Json\n\ntry {\n    $response = Invoke-RestMethod -Uri \"$myBaseURL/messages\" `\n        -Method Post `\n        -Headers @{ \"x-api-key\" = $apiKey; \"anthropic-version\" = \"2023-06-01\"; \"Content-Type\" = \"application/json\" } `\n        -Body $testPayload `\n        -SkipCertificateCheck\n    Write-Host \"🚀 测试成功！模型回复: $($response.content[0].text)\" -ForegroundColor Cyan\n} catch {\n    Write-Error \"❌ 连接依然失败。错误信息: $_\"\n    Write-Host \"提示：请检查你的代理软件是否开启了 TUN 模式，或者中转域名是否拼写正确（是否需要加 /v1）。\" -ForegroundColor Yellow\n}\n\n为什么这次能行？\n\n兼容性赋值：使用了 Add-Member，即便你的配置文件里原本没写过 Base URL，脚本也会强行创建这个配置项。\n安全协议强制对齐：通过 [Net.ServicePointManager]::SecurityProtocol 显式指定使用 TLS 1.2/1.3，这通常能解决截图末尾出现的 unexpected EOF（握手阶段被强踢）问题。\n静默操作：运行完后，你直接重启 VS Code，Cloud Code 对接 DC 模型的 Base URL 就会生效，不需要再手动翻找设置。\n\n操作建议：运行前确保你的代理软件（如 Clash）处于开启状态。如果运行后依然报 EOF，请尝试更换代理软件的节点。\n\n---\n*从 Gemini 导出，2026-05*\n\n---\n*Gemini · 系统运维 / PowerShell · 2026-05-08*\n"}