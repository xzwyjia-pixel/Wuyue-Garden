---
title: Prompted
source: gemini
date: 2026-05-08
category: 文案创意
old_category: 编程开发
tags: [gemini, 文案创意]
---

## Prompted

## Prompted

明白，完全跳出 VS Code 的配置逻辑，我们直接在 PowerShell (pwsh) 终端里通过环境变量和 CLI 参数来实现 Cloud Code 与 DC 大模型的无缝对接。
从你最后一张截图（image_606b5f.png）看，由于 $testCmd 中的双引号嵌套导致 iex 验证失败了。
请直接在你的 PowerShell 窗口中粘贴并运行以下代码（注意：只需修改前三行）。这套脚本采用了更鲁棒的 ArgumentList 方式，彻底解决引号冲突和 EOF 问题：
# 1. 基础参数配置
$myBaseURL = "https://你的中转域名.com/v1"
$apiKey = "你的_API_KEY"
$proxy = "http://127.0.0.1:7890"

# 2. 注入终端环境变量 (让 Cloud Code CLI 能够识别 DC 模型)
$env:HTTP_PROXY = $proxy
$env:HTTPS_PROXY = $proxy
$env:ANTHROPIC_BASE_URL = $myBaseURL
$env:ANTHROPIC_API_KEY = $apiKey
$env:NODE_TLS_REJECT_UNAUTHORIZED = "0"

Write-Host "✅ 当前会话环境变量已注入。" -ForegroundColor Green

# 3. 自动化验证连接 (改用更稳定的方式处理 curl 参数)
$testUrl = if ($myBaseURL -like "*/v1*") { "$myBaseURL/messages" } else { "$myBaseURL/v1/messages" }
$jsonPayload = '{"model": "claude-3-5-sonnet-20240620", "max_tokens": 10, "messages": [{"role": "user", "content": "hi"}]}'

Write-Host "📡 正在验证终端与 DC 大模型的连接..." -ForegroundColor Yellow

# 使用 Start-Process 配合 ArgumentList，完美避开引号解析错误
$curlArgs = @("-k", "-s", "-X", "POST", $testUrl, 
              "-H", "x-api-key: $apiKey", 
              "-H", "anthropic-version: 2023-06-01", 
              "-H", "Content-Type: application/json", 
              "-d", $jsonPayload, 
              "-x", $proxy)

$response = & curl.exe @curlArgs

if ($response -like "*content*") {
    Write-Host "🚀 验证成功！你的 PowerShell 终端已对接 DC 大模型。" -ForegroundColor Green
    Write-Host "提示：请就在当前窗口执行你的 Cloud Code 或审计工具命令。" -ForegroundColor White
} else {
    Write-Host "❌ 验证返回空或错误。请确认：`n1. 中转域名是否拼写正确（$myBaseURL）`n2. 代理端口 $proxy 是否在运行。" -ForegroundColor Red
    $response | Write-Host
}

为什么这次能彻底解决你的问题？

环境变量即配置：Cloud Code 命令行工具和大多数基于 Python/Node 的审计工具都会自动读取 $env:ANTHROPIC_BASE_URL。只要这个变量存在，它们就会自动找 DC 大模型，而不是去连官方。
解决引号陷阱：你之前报错是因为 PowerShell 处理 curl 命令里的多层双引号时乱了。这次我用了数组传参（@curlArgs），这是 PowerShell 里最稳妥的调用外部程序的方式。
维持会话上下文：只要你不关掉这个 PowerShell 窗口，配置就一直有效。

操作建议：
执行完后，如果显示“🚀 验证成功”，你就可以在同一个窗口里直接输入你的 Cloud Code 审计命令（比如 cloud-code ... 或运行 python audit_tool.py），它会自动走这条已经打通的 DC 链路。

---
*从 Gemini 导出，2026-05*

---
*从 Gemini 导出 · 分类: 文案创意 · 2026-05*
