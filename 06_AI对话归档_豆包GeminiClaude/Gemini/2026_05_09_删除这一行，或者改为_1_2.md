{"content":"---\ntitle: \\(node:17008) Warning: Setting the NODE_TLS_REJECT_UNAUTHORIZED environment variable to '0' makes TLS connections and HTTPS requests insecure by disabling certificate verification.\n(Use `node --trace-warnings ...` to show where the warning was created)\n这个是什么意思？可否去除？\nsource: gemini\ndate: 2026-05-09\ncategory: 审计合规\ntags: [gemini, 审计合规]\n---\n\n## \\(node:17008) Warning: Setting the NODE_TLS_REJECT_UNAUTHORIZED environment variable to '0' makes TLS connections and HTTPS requests insecure by disabling certificate verification.\n(Use `node --trace-warnings ...` to show where the warning was created)\n这个是什么意思？可否去除？\n\n这个警告信息是 Node.js 环境中非常常见的安全提示，尤其是在处理 DeepSeek 直连 或 中转通道 等网络请求时。\n1. 这个警告是什么意思？\n简单来说，NODE_TLS_REJECT_UNAUTHORIZED = '0' 的设置告诉 Node.js：“在进行 HTTPS 请求时，跳过对 SSL 证书的安全性检查。”\n\n为什么会出现： 在你的 start-claude-*.ps1 脚本或系统环境变量中，为了确保能顺利通过某些特殊的中转站（GPTSAPI）或解决 ThinkPad P15V 上的网络代理拦截问题，可能手动关闭了证书验证。\n安全风险： 这样做会使连接容易受到“中间人攻击”（MITM），因为系统不再验证你连接的服务器是否真的是官方官网。\n\n2. 是否可以去除？\n可以去除，但在去除前需要确认你的网络环境。\n如果你的网络（直连 DeepSeek 官网）不需要特殊的证书跳过逻辑，你可以按照以下步骤“恢复安全模式”：\n方案 A：在脚本中关闭该设置（最直接）\n检查你的 start-claude-direct.ps1 脚本，如果看到这一行，请将其删除或将值改为 1：\n# 删除这一行，或者改为 1\n$env:NODE_TLS_REJECT_UNAUTHORIZED = \"1\" \n\n方案 B：清理系统全局变量\n如果脚本里没有这一行，说明它被设置在了 Windows 全局环境变量中：\n\n在 Windows 搜索框输入 “环境变量”，选择 “编辑系统环境变量”。\n点击 “环境变量” 按钮。\n在“用户变量”或“系统变量”列表中找到 NODE_TLS_REJECT_UNAUTHORIZED。\n选中它并点击 “删除”。\n\n3. 专家建议：什么时候必须留着它？\n考虑到你正在开发的 “规则甄查”系统 涉及到频繁的 API 调用：\n\n如果去除后报错（例如提示 CERT_HAS_EXPIRED 或 UNABLE_TO_VERIFY_LEAF_SIGNATURE）：说明你的网络环境（如公司防火墙或特定的中转通道）必须关闭验证才能连通。在这种情况下，建议保留，因为它目前是你业务通行的“豁免权”。\n如果去除后一切正常：那就果断去除，让你的 DeepSeek 直连模式 运行在更安全的标准 HTTPS 环境下。\n\n你可以先尝试在 start-claude-direct.ps1 中临时加上 $env:NODE_TLS_REJECT_UNAUTHORIZED = \"1\" 运行一下。如果没报红字错误，说明我们可以安全地永久移除它。\n\n---\n*从 Gemini 导出，2026-05*\n"}