{"content":"---\ntitle: 我现在没有在VS code里面执行这些命令，我现在所执行命令的位置是power sail。 你不要搞错了，我现在没有在vsco的里边执行所有的东西，我只是要配置一下终端Power sior，里边的Cloud code，这个环境与DC的大模型对接的。\nsource: gemini\ndate: 2026-05-08\ncategory: 系统运维\ntags: [gemini, 系统运维]\n---\n\n## 我现在没有在VS code里面执行这些命令，我现在所执行命令的位置是power sail。 你不要搞错了，我现在没有在vsco的里边执行所有的东西，我只是要配置一下终端Power sior，里边的Cloud code，这个环境与DC的大模型对接的。\n\n明白了，是我之前的理解偏向了 IDE 插件。既然你是在 原生 PowerShell (pwsh) 环境下运行 Cloud Code CLI（或者是通过命令行调用的审计工具），那么修改 settings.json 确实不是你的核心目标。\n你需要的是将 DC 大模型的中转配置 直接注入到你的 PowerShell 环境变量 和 Cloud Code 命令行上下文 中。\n直接在 PowerShell 窗口粘贴并运行以下程序，它会直接配置你的终端环境：\n# 1. 基础参数配置\n$myBaseURL = \"https://你的中转域名.com/v1\"\n$apiKey = \"你的_API_KEY\"\n$proxy = \"http://127.0.0.1:7890\"\n\n# 2. 自动化注入终端环境变量 (针对 Cloud Code CLI 和 DC 对接)\nWrite-Host \"🛠️ 正在配置 PowerShell 终端环境...\" -ForegroundColor Cyan\n\n# 设置通用代理变量\n$env:HTTP_PROXY  = $proxy\n$env:HTTPS_PROXY = $proxy\n\n# 注入 Cloud Code 和 Claude 识别的标准环境变量\n$env:ANTHROPIC_BASE_URL = $myBaseURL\n$env:ANTHROPIC_API_KEY  = $apiKey\n\n# 针对 Node.js 运行时（Cloud Code 底层）的 SSL 豁免\n$env:NODE_TLS_REJECT_UNAUTHORIZED = \"0\"\n\n# 3. 验证终端是否能够直连 DC 模型\nWrite-Host \"📡 正在验证终端与 DC 大模型的连接...\" -ForegroundColor Yellow\n\n$testUrl = if ($myBaseURL -like \"*/v1*\") { \"$myBaseURL/messages\" } else { \"$myBaseURL/v1/messages\" }\n\n# 使用 curl.exe 验证（避开 PowerShell 原生 netstack 的 EOF 问题）\n$testCmd = \"curl.exe -k -s -X POST `\"$testUrl`\" \" +\n           \"-H `\"x-api-key: $apiKey`\" \" +\n           \"-H `\"anthropic-version: 2023-06-01`\" \" +\n           \"-H `\"Content-Type: application/json`\" \" +\n           \"-d `\"{`\"model`\": `\"claude-3-5-sonnet-20240620`\", `\"max_tokens`\": 10, `\"messages`\": [{`\"role`\": `\"user`\", `\"content`\": `\"hi`\"}]}`\" \" +\n           \"-x $proxy\"\n\n$response = iex $testCmd\n\nif ($response -like \"*content*\") {\n    Write-Host \"✅ 验证成功！终端已成功对接 DC 大模型。\" -ForegroundColor Green\n    Write-Host \"现在你可以直接在当前 PowerShell 会话运行 Cloud Code 审计工具了。\" -ForegroundColor White\n} else {\n    Write-Host \"❌ 验证失败，返回结果：$response\" -ForegroundColor Red\n    Write-Host \"💡 请检查：1. 中转域名后缀是否正确；2. 代理端口 7890 是否开启。\" -ForegroundColor Yellow\n}\n\n为什么这对你的 PowerShell 终端有效？\n\n环境变量注入：Cloud Code 的命令行工具（CLI）通常会优先读取系统环境变量。通过 $env:ANTHROPIC_BASE_URL，你强制它不再访问官方域名，而是转投你的中转站。\n会话级生效：这段命令运行后，当前 PowerShell 窗口就变成了一个已经“调教”好的审计环境。\n避开 EOF 陷阱：继续使用 curl.exe 做验证，确保你能看到真实的中转站反馈，而不是 PowerShell 报错。\n\n执行建议：\n直接在你的 PowerShell 窗口粘贴运行。如果验证成功，你就在同一个窗口内继续运行你的 Cloud Code 或审计命令，环境变量会自动透传给下游程序。\n\n---\n*从 Gemini 导出，2026-05*\n"}