---
title: 需要，给我一份Claude可以直接执行的命令
source: gemini
date: 2026-05-10
category: 技术开发
subcategory: Python
old_category: 编程开发
tags: [gemini, 技术开发, Python]
---

## 需要，给我一份Claude可以直接执行的命令

## 需要，给我一份Claude可以直接执行的命令

为了确保清理过程既彻底又安全（不误伤你的 Python 环境和 AI 审计配置），我为你编写了这段 Python 自动化清理命令。
你可以直接将下方整个代码块复制并发送给 Claude Code，或者在你的 PowerShell 中直接运行。
📥 复制并发送给 Claude 的执行命令

请立即创建并执行以下 Python 脚本 smart_cleanup.py，用于清理 AppData 残留：
import os
import shutil
import stat
from pathlib import Path

# 1. 定义安全白名单（绝对不能删的文件夹）
WHITE_LIST = [
    'python', 'pip', 'claude', 'cursor', 'microsoft', 'apple', 'nvidia', 
    'torch', 'opencv', 'ghc', 'npm', 'roaming', 'google', 'anaconda', 'miniconda'
]

# 2. 定义已知流氓软件黑名单关键词
BLACK_LIST_KEYWORDS = ['360', 'coolfile', 'viewer', 'helper', 'assistant', 'zhushou']

def force_remove(path):
    """处理只读文件导致的权限问题"""
    def handle_error(func, path, exc_info):
        os.chmod(path, stat.S_IWRITE)
        func(path)
    if os.path.exists(path):
        shutil.rmtree(path, onerror=handle_error)

def scan_and_cleanup():
    appdata_paths = [os.getenv('APPDATA'), os.getenv('LOCALAPPDATA')]
    program_files = [os.environ.get('ProgramFiles'), os.environ.get('ProgramFiles(x86)')]
    
    to_delete = []
    print("🔍 正在扫描 AppData 残留...")

    for base_path in appdata_paths:
        if not base_path: continue
        for folder in os.listdir(base_path):
            folder_lower = folder.lower()
            folder_path = os.path.join(base_path, folder)
            
            if not os.path.isdir(folder_path): continue
            if any(safe in folder_lower for safe in WHITE_LIST): continue
            
            # 逻辑 A：黑名单直接判定
            if any(key in folder_lower for key in BLACK_LIST_KEYWORDS):
                to_delete.append(folder_path)
                continue
                
            # 逻辑 B：检查 C 盘主程序是否存在
            # 如果在 Program Files 中找不到同名目录，判定为残留
            exists_in_pc = any(os.path.exists(os.path.join(pf, folder)) for pf in program_files)
            
            if not exists_in_pc:
                # 排除掉 AppData 内部运行的合法程序
                # 这里我们采用保守策略：只有在黑名单内，或者你明确想删的才列入
                pass 

    if not to_delete:
        print("✅ 未发现明显流氓软件残留。")
        return

    print("\n🚩 发现以下疑似残留文件夹：")
    for p in to_delete:
        print(f"[待删] {p}")

    confirm = input("\n⚠️ 是否确认强制删除以上所有文件夹？(Y/N): ")
    if confirm.lower() == 'y':
        for p in to_delete:
            try:
                force_remove(p)
                print(f"🗑️ 已清理: {p}")
            except Exception as e:
                print(f"❌ 清理失败 {p}: {e}")
    else:
        print("操作已取消。")

if __name__ == "__main__":
    scan_and_cleanup()

执行要求：

先运行脚本进行扫描，并列出清单。
在删除前请务必让我进行人工确认（Y/N）。
运行完成后，请检查 E:\MyCodeProjects\logs\cleanup.log 并告诉我结果。



🛡️ 为什么这样操作最稳妥？

权限穿透：流氓软件常设“只读”属性防止被删，脚本中的 handle_error 逻辑会强制修改权限并暴力抹除，比你手动右键删除有用得多。
精准打击：我为你设置了 WHITE_LIST（白名单），确保你的 Python 库、Torch 环境、NVIDIA 驱动 等生产力工具不会被误伤。
针对性强：专门针对你提到的 360 和 Cool File Viewer 及其关联的“助手（Assistant/Helper）”关键词进行了扫描。

💡 建议
在发送给 Claude 运行之前，请确保你已经关闭了所有正在运行的 360 或 Cool File Viewer 窗口。如果删除失败，说明进程正在被占用，你可以让 Claude 接着写一行 taskkill /F /IM ... 的代码来强行杀掉它们。

---
*从 Gemini 导出，2026-05*

---
*Gemini · 技术开发 / Python · 2026-05-10*
