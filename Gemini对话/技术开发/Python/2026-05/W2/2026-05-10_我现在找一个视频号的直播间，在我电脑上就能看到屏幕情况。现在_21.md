---
title: 我现在找一个视频号的直播间，在我电脑上就能看到屏幕情况。现在开始i测试。告诉我给Claude发送什么指令
source: gemini
date: 2026-05-10
category: 技术开发
subcategory: Python
old_category: 编程开发
tags: [gemini, 技术开发, Python]
---

## 我现在找一个视频号的直播间，在我电脑上就能看到屏幕情况。现在开始i测试。告诉我给Claude发送什么指令

## 我现在找一个视频号的直播间，在我电脑上就能看到屏幕情况。现在开始i测试。告诉我给Claude发送什么指令

既然你已经准备好了直播间画面，我们要分两步走：先让 Claude 把环境和代码彻底搞定，然后再启动“实战监控”。
请直接将以下指令分次复制并发送给你的 Claude Code 终端：
第一步：环境修复与文件创建（核心指令）
复制并发送这段文字给 Claude：

指令：

修复环境： 请在终端运行 pip install "pillow<12.0" "numpy==1.26.4" 解决依赖冲突。
创建脚本： 在当前目录下创建 monitor_view.py。

功能：使用 pyautogui 每 30 秒截取一次评论区（默认坐标设为 (1450, 200, 450, 750)，稍后我会手动微调）。
功能：使用 easyocr 识别文字并以时间戳追加到 chat_log.json。
功能：每 5 分钟截取全景图存入 screenshots/ 文件夹。


创建坐标测试工具： 创建 test_coords.py，运行后截取当前设定的 chat_area 并保存为 check.png，同时打印识别到的第一行文字。
创建分析逻辑： 创建 analyze_stream.py，用于读取 chat_log.json 并统计关键词（多少钱、怎么买、哈哈、海燕、姐）出现的频率。



第二步：校准坐标（最重要的一环）
当 Claude 完成上述操作后，请在你的 PowerShell 中运行：
python test_coords.py

看结果： 检查目录下的 check.png。

如果没框中评论区：请手动修改 monitor_view.py 顶部的 CONFIG 坐标。
如果框中了：直接进入下一步。


第三步：开始实战监控
在 PowerShell 中输入：
python monitor_view.py


🧐 验证期间你该观察什么？（找出改进点）
脚本跑起来后，请你一边看直播，一边留意以下三点，这些将是你稍后要丢给 Claude 改进的需求：

OCR 准确率： 观察控制台输出。如果视频号的弹幕背景导致文字识别不出（比如把“多少钱”认成“多小线”），你需要告诉 Claude：“增加图像预处理，提升 OCR 锐度。”
消息去重： 因为视频号评论滚动较慢，同一条消息可能被截到好几次。你需要告诉 Claude：“增加去重逻辑，不要记录重复的弹幕。”
冲突捕捉： 当主播之间发生互动（比如类似“海燕式”的幽默吵架）时，看公屏是否有“哈哈”或“太逗了”。如果没有识别到，你需要告诉 Claude：“扩展情感词库，捕捉直播间的综艺感。”

现在，请先发第一段指令给 Claude，让它把“监控地基”打好！它完成后告诉我，我教你如何根据 check.png 快速调坐标。

---
*从 Gemini 导出，2026-05*

---
*Gemini · 技术开发 / Python · 2026-05-10*
