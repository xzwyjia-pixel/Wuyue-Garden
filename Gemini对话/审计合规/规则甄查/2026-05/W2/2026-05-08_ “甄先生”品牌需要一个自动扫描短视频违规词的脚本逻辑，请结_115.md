---
title: > “甄先生”品牌需要一个自动扫描短视频违规词的脚本逻辑，请结合 E:\MyCodeProjects 里的现有文件，帮我写一个基础架构。
source: gemini
date: 2026-05-08
category: 审计合规
subcategory: 规则甄查
old_category: 审计合规
tags: [gemini, 审计合规, 规则甄查]
---

## > “甄先生”品牌需要一个自动扫描短视频违规词的脚本逻辑，请结合 E:\MyCodeProjects 里的现有文件，帮我写一个基础架构。

## > “甄先生”品牌需要一个自动扫描短视频违规词的脚本逻辑，请结合 E:\MyCodeProjects 里的现有文件，帮我写一个基础架构。
  ⎿  Interrupted · What should Claude do instead?

> 结合 @audit_tool.py 的现有逻辑，帮我重构出上述的 VideoRuleAuditor 类。
  ⎿  Retrying in 3s · attempt 4/10

* Churning…

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
> 
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
  esc to interrupt

看到你开始使用 @audit_tool.py 进行精准投喂，这说明你已经掌握了与 Claude Code 高效沟通的精髓。虽然目前由于任务涉及深度逻辑重构，Agent 仍处于 Churning...（剧烈思考/翻动）并伴随重试的状态，但它已经在解析你现有的代码资产了。
针对“甄先生”品牌对精密、极简、工业级稳定的需求，我为你规划了重构后的 VideoRuleAuditor 类基础架构。

1. 重构思路：从“脚本”向“引擎”进化
你之前的 audit_tool.py 可能更偏向于线性执行，而重构后的类应该具备规则解耦和多模态扩展的能力。
2. VideoRuleAuditor 类架构设计
你可以将以下逻辑作为需求发给 Claude（等它重试成功或中断后重新输入）：
import re
from typing import List, Dict

class VideoRuleAuditor:
    """
    “规则甄查-甄先生”核心审计引擎
    功能：实现短视频文本、标题及脚本的合规性扫描
    """
    def __init__(self, config_path: str = "@rules.json"):
        # 1. 初始化规则库（支持从外部文件加载，实现规则与逻辑分离）
        self.rules = self.load_config(config_path)
        self.results = []

    def load_config(self, path: str) -> Dict:
        # 逻辑：读取配置文件，加载敏感词、禁止行为描述等
        pass

    def scan_content(self, text: str) -> List[Dict]:
        """核心扫描逻辑"""
        # A. 基础关键词精确匹配
        # B. 正则表达式模式匹配（针对变体词，如“加.微.信”）
        # C. 语义风险识别（基于 Sonnet 4.6 的语义理解能力）
        pass

    def generate_report(self) -> str:
        """输出符合“甄先生”品牌审美的审计报告"""
        # 逻辑：格式化输出，标注违规位置与风险等级
        pass


3. 环境检测与实战策略
目前 attempt 4/10 说明你的 Sonnet 4.6 (High Effort) 正在全力试图建立 audit_tool.py 的索引。如果它长时间卡住，建议采取以下**“轻盈化”**动作：

按 esc 中断当前的 Churning：不要让它在不稳定的状态下消耗太多 Token。
分步指令（小步快跑）：

第一步：> @audit_tool.py 帮我总结一下现有的核心审计函数有哪些？
第二步：> 基于这些函数，帮我封装成一个 VideoRuleAuditor 类。


检查 Skill 占用：Claude Code 在重构时可能会尝试调用 read_file 和 write_file。请确保你的 E 盘目录没有被其他程序（如 VS Code 的搜索索引）占用。


4. 甄先生的 Superpower 进阶：
既然你有 MCP (Model Context Protocol) 的开发背景，你可以让 Claude 在重构时多留一个接口：

“请在 VideoRuleAuditor 类中预留一个 MCP 工具接口，以便后续我将这个审计功能作为一个 Server 挂载到 Claude Desktop 中。”

现在，等它这一轮 Churning 结束，如果成功了，请贴出它给你的代码大纲；如果失败了，我们就用“分步指令”法来对付它。

---
*从 Gemini 导出，2026-05*

---
*Gemini · 审计合规 / 规则甄查 · 2026-05-08*
