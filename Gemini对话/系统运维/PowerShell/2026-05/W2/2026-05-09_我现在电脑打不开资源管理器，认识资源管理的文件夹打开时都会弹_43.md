---
title: 我现在电脑打不开资源管理器，认识资源管理的文件夹打开时都会弹出附件的这个窗口，我该如何解决这个问题
source: gemini
date: 2026-05-09
category: 系统运维
subcategory: PowerShell
old_category: 系统运维
tags: [gemini, 系统运维, PowerShell]
---

## 我现在电脑打不开资源管理器，认识资源管理的文件夹打开时都会弹出附件的这个窗口，我该如何解决这个问题

## 我现在电脑打不开资源管理器，认识资源管理的文件夹打开时都会弹出附件的这个窗口，我该如何解决这个问题

看到你上传的截图了，这种“该文件没有与之关联的应用来执行该操作”的错误，通常是因为系统的文件夹关联注册表信息被误删或被某些软件篡改导致的。
既然现在无法通过常规方式打开文件夹，我们可以尝试通过以下几种由易到难的方法来修复：
方法一：重建 Explorer.exe 进程
有时候这只是资源管理器进程的暂时性逻辑错误。

按下 Ctrl + Shift + Esc 打开任务管理器。
找到“Windows 资源管理器”，右键点击它并选择“重新启动”。
如果列表中没有，点击左上角的“运行新任务”，输入 explorer.exe 并回车。


方法二：使用命令提示符修复（最有效）
由于你现在无法打开文件夹，通过命令行修复注册表关联是最直接的办法。

按下 Win + R 键，输入 cmd，然后按 Ctrl + Shift + Enter 以管理员身份运行。
在黑色窗口中依次输入以下两条命令（每输完一条按一次回车）：

第一条（修复文件夹关联）：
assoc .folder=Folder


第二条（修复目录关联）：
assoc .directory=Directory




重启电脑，看是否恢复正常。


方法三：通过 PowerShell 重置系统应用
如果上述命令无效，可以尝试重置 Windows 默认关联设置。

右键点击“开始”按钮，选择“终端管理员”或“Windows PowerShell (管理员)”。
复制并粘贴以下命令后回车：

   Get-AppXPackage -AllUsers | Foreach {Add-AppxPackage -DisableDevelopmentMode -Register "$($_.InstallLocation)\AppXManifest.xml"}

注意：运行过程中可能会出现红色错误提示，这是正常的，等它运行结束即可。

方法四：修改注册表（进阶操作）
如果前几种方法都没用，可能是注册表核心键值丢失了。

按下 Win + R，输入 regedit 并回车打开注册表编辑器。
导航到以下路径：
HKEY_CLASSES_ROOT\Directory\shell
在右侧检查 (默认) 数值。正常情况下，它的数值数据应该是空的或者 none。如果显示的是其他内容（比如某些解压缩软件的名字），请右键点击“(默认)”选择“修改”，将数值清空。


建议：
你提到你使用的是 ThinkPad P15V 开展技术工作，建议在修复后，检查一下近期安装的工业自动化软件或系统优化工具，看是否是它们在安装过程中修改了系统的底层路径。
如果以上方法都无法解决，可能需要考虑使用 Windows 的“系统还原”功能，回到正常工作的时间点。
解决过程中遇到任何具体报错，可以随时告诉我。

---
*从 Gemini 导出，2026-05*

---
*Gemini · 系统运维 / PowerShell · 2026-05-09*
