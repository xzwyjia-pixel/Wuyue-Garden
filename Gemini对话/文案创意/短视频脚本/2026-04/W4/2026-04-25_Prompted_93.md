---
title: Prompted
source: gemini
date: 2026-04-25
category: 文案创意
subcategory: 短视频脚本
old_category: 文案创作
tags: [gemini, 文案创意, 短视频脚本]
---

## Prompted

## Prompted

从你的截图来看，ftype 命令执行正常，且注册表的“映像劫持”名单中并没有发现明显的 explorer.exe 拦截项。这说明系统文件关联的大框架没问题，但局部针对“资源管理器”的调用依然被阻塞。
既然常规修复未能即时生效，我们直接通过一个针对注册表底层协议的专用修复程序来强制重置所有文件关联。
自动化协议修复程序
请再次创建一个批处理文件（记得保存为 ANSI 编码），或者在管理员 CMD 窗口中依次复制执行以下代码：
@echo off
echo 正在执行系统底层协议强制修复...

:: 修复文件夹与目录打开关联
reg add "HKCR\Directory\shell\open\command" /ve /t REG_EXPAND_SZ /d "%%SystemRoot%%\Explorer.exe" /f
reg add "HKCR\Drive\shell\open\command" /ve /t REG_EXPAND_SZ /d "%%SystemRoot%%\Explorer.exe" /f

:: 修复系统文件夹协议
reg add "HKCR\Folder\shell\open\command" /ve /t REG_EXPAND_SZ /d "%%SystemRoot%%\Explorer.exe" /f

:: 彻底清除快捷方式的 UserChoice 锁定（解决“没有关联应用”报错的核心）
reg delete "HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\FileExts\.lnk" /f
reg delete "HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\FileExts\.exe" /f

:: 重启资源管理器
taskkill /f /im explorer.exe
start explorer.exe

echo 修复完成，请按 Win+E 测试。
pause


如果上述脚本依然报错，请尝试以下“手动救急”操作：
由于报错提示“没有与之关联的应用”，这通常是 Windows 内部的 “默认应用托管服务” 出现了逻辑死锁。


重置默认打开方式：

随便找一个 .txt 文件（或新建一个）。
右键点击 -> 打开方式 -> 选择其他应用。
不管现在选的是什么，点击底部的 “选择电脑上的应用”。
定位到 C:\Windows\explorer.exe，并勾选 “始终使用此应用打开 .txt 文件”。
注：这样做是为了强制唤醒 explorer.exe 在系统关联中的活跃度。



通过任务管理器直接启动：

按下 Ctrl + Shift + Esc 打开任务管理器。
点击 “运行新任务”。
在输入框直接输入：shell:RecycleBinFolder 然后按回车。
如果这样能打开回收站，说明资源管理器内核没死，只是外壳关联坏了。



最后的一点洞察：
你在注册表截图中打开了 Image File Execution Options，请确认你不仅检查了 HKEY_LOCAL_MACHINE，也检查了 HKEY_CURRENT_USER 下对应的路径。有时劫持只发生在当前用户下。
如果这些手段都用过了还是跳出那个错误对话框，建议你按下 Win + I 进入 设置 -> 系统 -> 恢复 -> 立即重新启动（高级启动）。在重启后的蓝色菜单中选择 “启动修复”。系统会自动检测这种严重的关联丢失。

---
*从 Gemini 导出，2026-04*

---
*Gemini · 文案创意 / 短视频脚本 · 2026-04-25*
