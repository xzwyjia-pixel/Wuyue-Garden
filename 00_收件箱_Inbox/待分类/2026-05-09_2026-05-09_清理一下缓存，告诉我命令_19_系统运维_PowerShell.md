{"content":"---\ntitle: 清理一下缓存，告诉我命令\nsource: gemini\ndate: 2026-05-09\ncategory: 系统运维\nsubcategory: PowerShell\nold_category: 系统运维\ntags: [gemini, 系统运维, PowerShell]\n---\n\n## 清理一下缓存，告诉我命令\n\n## 清理一下缓存，告诉我命令\n\n清理缓存的具体命令取决于你想要清理的对象（是 Python 编译文件、Pip 包、Git 冗余，还是系统临时文件）。根据你刚才的操作环境，这里有几个最常用的清理指令：\n1. 清理 Python 编译缓存 (__pycache__)\n如果你在重构项目后发现导入路径还是报错，通常是由于旧的 .pyc 文件在作祟。\n\nWindows (PowerShell):\nGet-ChildItem -Path . -Filter \"__pycache__\" -Recurse | Remove-Item -Force -Recurse\n\n\nWindows (CMD):\n\n    for /d /r . %d in (__pycache__) do @if exist \"%d\" rd /s /q \"%d\"\n    ```\n\n### 2. 清理 Pip 缓存\n如果你觉得安装包占用空间太大，或者想重新下载依赖：\n```bash\npython -m pip cache purge\n\n3. 清理 Pytest 缓存\nPytest 会生成 .pytest_cache 目录来存储上次失败的测试状态，清理它可以让测试从头开始：\npython -m pytest --cache-clear\n\n4. Git 强力清理 (慎用)\n如果你刚才的 git mv 留下了一些不在跟踪范围内的垃圾文件：\n\n查看将被删除的文件: git clean -fdn\n确认删除: git clean -fd\n\n5. 清理 Windows 系统临时文件\n刚才你在 E:/MyCodeProjects 下清理了一个临时脚本，如果想清理系统级的：\n\n运行命令: 按 Win + R 输入 %temp%，然后全选删除即可。\n\n\n刚才 skill-creator 写入失败的小建议：\n报错通常是因为文件夹不存在。你可以执行：\nmkdir -p \"E:/MyCodeProjects/02-SubProjects/skill-creator\"\n\n然后再运行写入 README 的指令，这样缓存和路径就都通顺了。\n你想针对哪个特定的工具（比如 Playwright 或 Obsidian）进行深度清理吗？\n\n---\n*从 Gemini 导出，2026-05*\n\n---\n*Gemini · 系统运维 / PowerShell · 2026-05-09*\n"}