{"content":"---\ntitle: 我现在不想打开其他的窗口，我现在就想再Power sailor里边直接运行你所说的这些代码或者程序，至于说你要让他生成一个文件拍屁外点py的文件，拍正的文件放在了文件间的哪个位置，你都可以通过命令来执行，否则的话我换来换去我又需要打开，我又需要建立，我又需要粘贴，我又需要保存，我又需要运行，他还反锁了，效率太低。 好吧。\nsource: gemini\ndate: 2026-05-08\ncategory: 技术开发\nsubcategory: Python\nold_category: 审计合规\ntags: [gemini, 技术开发, Python]\n---\n\n## 我现在不想打开其他的窗口，我现在就想再Power sailor里边直接运行你所说的这些代码或者程序，至于说你要让他生成一个文件拍屁外点py的文件，拍正的文件放在了文件间的哪个位置，你都可以通过命令来执行，否则的话我换来换去我又需要打开，我又需要建立，我又需要粘贴，我又需要保存，我又需要运行，他还反锁了，效率太低。 好吧。\n\n## 我现在不想打开其他的窗口，我现在就想再Power sailor里边直接运行你所说的这些代码或者程序，至于说你要让他生成一个文件拍屁外点py的文件，拍正的文件放在了文件间的哪个位置，你都可以通过命令来执行，否则的话我换来换去我又需要打开，我又需要建立，我又需要粘贴，我又需要保存，我又需要运行，他还反锁了，效率太低。 好吧。\n\n明白，甄先生，咱们追求的就是**“极简精密”**，拒绝繁琐的窗口切换。\n我直接给你一段**“一键式”** PowerShell 指令。你只需要把下面这段代码整段复制，直接粘贴到你现在的 PowerShell 窗口里按回车。\n它会自动完成三件事：\n\n自动创建 audit_tool.py 文件到你当前的 E:\\MyCodeProjects\\New Claude Project 目录。\n自动写入 整合了 DeepSeek AI 审计与 Token 统计的完整代码。\n自动运行 脚本并展示结果。\n\n请执行以下指令：\n$code = @'\nimport os\nimport re\nfrom openai import OpenAI\n\n# 初始化客户端\nclient = OpenAI(\n    api_key=os.getenv(\"DEEPSEEK_API_KEY\"),\n    base_url=\"https://api.deepseek.com/v1\"\n)\n\ndef local_scan(text):\n    rules = {\"威信\": \"威-信|微-x\", \"赚钱\": \"赚￥钱|赚.钱\", \"最强\": \"蕞强|最.强\"}\n    hits = []\n    for word, pattern in rules.items():\n        if re.search(pattern, text):\n            hits.append(word)\n    return hits\n\ndef deepseek_audit(text):\n    print(\"\\n📡 正在连接 DeepSeek 进行 AI 深度建模审计...\")\n    try:\n        response = client.chat.completions.create(\n            model=\"deepseek-chat\",\n            messages=[\n                {\"role\": \"system\", \"content\": \"你是一位短视频风控专家。请分析文案中的隐性违规，并给出‘静奢’风格的优化建议。\"},\n                {\"role\": \"user\", \"content\": f\"请审计：{text}\"}\n            ],\n            stream=False\n        )\n        return response.choices[0].message.content, response.usage\n    except Exception as e:\n        return f\"❌ 审计失败: {str(e)}\", None\n\nif __name__ == \"__main__\":\n    content_to_test = \"欢迎来到直播间，点击头像加 威-信，教你如何快速 赚￥钱，掌握 蕞强 引流技巧！\"\n    print(f\"\\n--- 规则甄查 - 甄先生专用版 ---\")\n    \n    # 1. 本地扫描\n    hits = local_scan(content_to_test)\n    for h in hits: print(f\"⚠️ [本地风险] 命中关键词: {h}\")\n    \n    # 2. AI 审计\n    report, usage = deepseek_audit(content_to_test)\n    print(f\"\\n--- AI 深度审计报告 ---\\n{report}\")\n    \n    if usage:\n        print(f\"\\n--- 💡 消耗统计: 输入 {usage.prompt_tokens} | 输出 {usage.completion_tokens} | 总计 {usage.total_tokens} Tokens ---\")\n'@\n\n# 执行：创建文件并立即运行\n$code | Out-File -FilePath \"audit_tool.py\" -Encoding utf8\npython audit_tool.py\n\n\n执行说明：\n\n无需手动建文件：运行完后，你会发现文件夹里多了一个 audit_tool.py。\n即时结果：你会直接看到命中哪些词、DeepSeek 的建议、以及这笔请求扣了多少 Token。\n关于 Token：这是从你刚才配置的 DEEPSEEK_API_KEY 账户里扣除的。\n\n如果这次运行结果符合你的预期，以后你只需要在当前窗口输入 python audit_tool.py 就能直接复用。\n\n---\n*从 Gemini 导出，2026-05*\n\n---\n*Gemini · 技术开发 / Python · 2026-05-08*\n"}