{"content":"---\ntitle: 请帮我本地部署 GPT-SoVIIS项目。\nsource: gemini\ndate: 2026-05-03\ncategory: 系统运维\nold_category: 系统运维\ntags: [gemini, 系统运维]\n---\n\n## 请帮我本地部署 GPT-SoVIIS项目。\n\n## 请帮我本地部署 GPT-SoVIIS项目。\n1、项目地址:K**\n2、目标路径:目录D:git_project\\GPI_SoVIIS我已经创建好了，请直接进入该目录操作。\n3、环境要求:\n务必先在当前目录下创建一个Python 虚拟环境(venv)并激活，绝不要污染全局环境。*安装Python 依赖时，请强制使用清华源\n4、模型下载:\n请优先尝试从ModelScope获取对应的GPI-SoVIIS模型文件。\n5、任务目标:安装完成后，请尝试帮我运行webui.py验证是否成功。\n\n既然你已经准备好了目录 D:\\git_project\\GPT-SoVITS（修正了路径中的拼写），我们将严格按照你的极简精密要求，在不污染全局环境的前提下，完成本地部署。\n由于我无法直接操控你的本地文件系统，请你在 PowerShell 或 CMD 中依次执行以下经过优化的指令。\n第一阶段：项目克隆与环境初始化\n请打开终端，执行以下命令：\n# 1. 进入目标目录\ncd /d D:\\git_project\\GPT-SoVITS\n\n# 2. 克隆项目（如果尚未克隆）\ngit clone https://github.com/RVC-Boss/GPT-SoVITS.git .\n\n# 3. 创建 Python 虚拟环境 (venv)\npython -m venv venv\n\n# 4. 激活虚拟环境\n# Windows (PowerShell):\n.\\venv\\Scripts\\Activate.ps1\n# Windows (CMD):\n.\\venv\\Scripts\\activate.bat\n\n\n第二阶段：依赖库强制加速安装\n激活环境后（命令行前缀应出现 (venv) 字样），执行依赖安装。我们强制使用清华源并针对 Windows 常见的 Torch 环境进行优化：\n# 1. 升级 pip\npython -m pip install --upgrade pip -i https://pypi.tuna.tsinghua.edu.cn/simple\n\n# 2. 安装 Torch 相关核心组件（强制清华源）\npip install torch torchvision torchaudio --index-url https://pypi.tuna.tsinghua.edu.cn/simple\n\n# 3. 安装项目所有其余依赖\npip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple\n\n\n第三阶段：模型获取 (ModelScope)\nGPT-SoVITS 需要预训练权重才能运行。由于 ModelScope 结构较为精密，建议使用 Python 脚本定向下载，避免手动移动文件的繁琐：\n# 安装 ModelScope 客户端\npip install modelscope -i https://pypi.tuna.tsinghua.edu.cn/simple\n\n# 运行下载脚本（下载到当前目录的 GPT_SoVITS/pretrained_models）\npython -c \"from modelscope import snapshot_download; snapshot_download('iic/GPT-SoVITS', local_dir='GPT_SoVITS/pretrained_models')\"\n\n\n第四阶段：验证运行\n安装完成后，尝试启动 WebUI。该项目通常通过 webui.py 提供交互界面：\n# 尝试启动 WebUI\npython webui.py\n\n关键检查点与建议\n\nFFmpeg 环境： GPT-SoVITS 的音频处理极度依赖 FFmpeg。请确保你的系统已安装 FFmpeg 并且 ffmpeg 命令在环境变量中可用。如果没有，请下载后将其 bin 目录路径加入系统变量。\n显存注意： 该项目对显存有一定要求。如果启动报错 Out of Memory，请检查是否有其他 AI 工具（如 Ollama 或其他本地模型）占用了显存。\n编码纠纷： 如果在运行 webui.py 时遇到中文乱码或路径字符报错，请确保你的系统区域设置支持 UTF-8。\n\n如果在执行 python webui.py 后终端输出了类似 [http://127.0.0.1:9874](http://127.0.0.1:9874) 的地址，说明部署已经成功。下一步你是否需要我帮你写一个自动整理音频素材并生成微调数据集的脚本？\n\n---\n*从 Gemini 导出，2026-05*\n\n---\n*从 Gemini 导出 · 分类: 系统运维 · 2026-05*\n"}