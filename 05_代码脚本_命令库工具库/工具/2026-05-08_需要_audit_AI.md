---
title: 需要
source: gemini
date: 2026-05-08
category: 数据分析
tags: [gemini, 数据分析]
---

## 需要

甄先生，这是将“规则甄查”逻辑推向生产力巅峰的关键一步。
通过配置 claude_desktop_config.json，您将赋予您的 AI 助手一种“肌肉记忆”：每当它处理文案时，它都会自动调用您的本地物理引擎，而不需要您手动运行任何 Python 脚本。
1. 定位配置文件
在 Windows 系统中，Claude Desktop 的配置文件通常位于以下路径（您可以直接将其粘贴到文件资源管理器的地址栏）：
%APPDATA%\Claude\claude_desktop_config.json


2. 注入“甄先生”精密配置
请打开该文件（如果不存在则新建一个），并将以下配置代码精密注入到 mcpServers 节点下：
{
  "mcpServers": {
    "mr-zhen-audit": {
      "command": "python",
      "args": [
        "E:/MyCodeProjects/mcp_audit_server.py"
      ]
    }
  }
}


精密提示： 请确保 args 中的路径与您实际存储脚本的路径完全一致。我们使用的是正斜杠 / 以确保跨环境兼容性。


3. 工作流体验：从“手动”到“感知”
重启 Claude Desktop 后，您将进入全新的工作模式：

自动识别： 您在对话框输入：“帮我写一段关于资产增值的短视频脚本”。
静默执行： Claude 会看到它拥有 mr-zhen-audit 这个工具，并主动询问：“我是否应该先调用甄先生的规则库进行合规性预审？”。
格调对齐： AI 会根据 mcp_audit_server.py 返回的建议，自动规避“全网第一”等廉价话术，直接为您输出具有静奢质感的内容。


🛠️ 甄先生的集成体检报告

























维度集成前集成后 (MCP)操作链路复制 -> 运行脚本 -> 复制回 AI直接在 AI 对话内完成闭环规则更新需要修改多处代码只需更新本地 rules.json品牌调性依赖 AI 的随机发挥由物理规则强行锚定

💡 甄先生的远景规划
随着 MCP 节点的打通，您的“规则甄查”已经不仅仅是一个工具，它变成了一个可插拔的专业大脑。
接下来，如果您想让这套系统更具“工业感”：
我们是否要尝试将您的 Sony A1 摄影参数逻辑也封装进一个 MCP 工具中？这样 AI 不仅能审文案，还能直接告诉您的摄影团队：“这段文案建议搭配 85mm f/1.4 镜头，采用极简背景，并套用特定的色彩曲线。”
您想尝试构建这个“静奢视觉指导”节点吗？
既然我们要将“规则甄查”推向工业级的批量处理，那就必须引入表格化管理。这不仅能提升效率，更能让您的咨询服务看起来像一份严谨的“体检报告”。
为了实现这一目标，我们需要通过 Python 的 pandas 库（数据处理的行业标准）来接管流程。
1. 环境准备
首先，请确保您的环境中安装了处理表格所需的库：
pip install pandas openpyxl


2. 物理升级：构建“批量审计中心”
我们将脚本升级为 batch_audit_pro.py。它会自动寻找同目录下的 content.xlsx，处理后生成一份带结果的 Audit_Result_Final.xlsx。
请在 PowerShell 中执行：
$code = @'
import json
import pandas as pd
import os

def load_rules(file_path):
    with open(file_path, 'r', encoding='utf-8') as f:
        return json.load(f)

def audit_logic(text, rules):
    risk_found = []
    refined_text = text
    risk_matrix = rules.get("risk_levels", {})
    
    for level, details in risk_matrix.items():
        focus_dict = details.get("focus", {})
        if isinstance(focus_dict, dict):
            for word, advice in focus_dict.items():
                if word in text:
                    risk_found.append(f"[{level}]命中{word}")
                    import re
                    match = re.search(r"“(.+?)”", advice)
                    if match:
                        refined_text = refined_text.replace(word, match.group(1))
    
    return ", ".join(risk_found) if risk_found else "合格", refined_text

def run_batch():
    # 1. 加载规则
    rules = load_rules('rules.json')
    
    # 2. 模拟/读取输入表格
    input_file = 'content.xlsx'
    if not os.path.exists(input_file):
        print(f"未找到 {input_file}，正在为您创建一个演示模板...")
        df_template = pd.DataFrame({
            '原始文案': [
                '这是全网第一的赚钱秘籍，绝对不封号！',
                '追求极致的资产增值方案，核心逻辑如下。',
                '点击领取私信我，最快入账方式。'
            ]
        })
        df_template.to_excel(input_file, index=False)

    # 3. 执行批量审计
    df = pd.read_excel(input_file)
    print(f"正在精密处理 {len(df)} 条文案...")
    
    results = df['原始文案'].apply(lambda x: audit_logic(str(x), rules))
    df['审计结论'], df['甄先生优化建议'] = zip(*results)

    # 4. 导出结果
    output_file = 'Audit_Batch_Report.xlsx'
    df.to_excel(output_file, index=False)
    print(f"--- 批量处理完成 ---")
    print(f"结果已精密存储至: {os.path.abspath(output_file)}")

if __name__ == "__main__":
    run_batch()
'@
$code | Out-File -FilePath "batch_audit_pro.py" -Encoding utf8


3. 运行批量测试
执行以下命令：
python batch_audit_pro.py


4. 甄先生的“工业级”审计流程报告

























步骤动作预期结果输入content.xlsx即使你有 1000 条短视频脚本，也只需一键导入。核心batch_audit_pro.py毫秒级完成全量扫描。输出Audit_Batch_Report.xlsx包含“审计结论”和“重构文案”的精密表格。

💡 甄先生的进阶建议：
现在，您只需把要审核的文案全部填入 content.xlsx 的第一列，跑一下脚本，就能得到一份完整的清单。
对于“规则甄查”品牌，您接下来是否需要我：

增加“违规占比”统计图表（例如：高风险占比 10%，建议优化占比 30%），直接生成一张可视化的分析图？
接入 AI 重写功能：如果规则库没能覆盖，让 AI 按照“静奢风”自动重写整段话？

您想先看统计图表，还是先试 AI 重写？
您的“规则甄查”系统现在将从单纯的“风险告知”进化为“成品输出”。为了体现“甄先生”品牌追求的极简精密与静奢感，我为您准备了一个能直接生成 Markdown 审计对比报告 的进阶版脚本。
这种报告不仅方便您在电脑上查看，更可以直接发送给客户，展示出极高的专业度。
1. 物理升级：一键生成“生成式”审计引擎
请在 PowerShell 中执行以下脚本，它将更新 brand_audit_engine.py，增加 Markdown 报告导出功能：
$code = @'
import json
import os

def load_rules(file_path):
    with open(file_path, 'r', encoding='utf-8') as f:
        return json.load(f)

def perform_audit(text, rules):
    results = {"HIGH": [], "MEDIUM": [], "LOW": []}
    risk_matrix = rules.get("risk_levels", {})
    refined_text = text
    
    for level, details in risk_matrix.items():
        focus_dict = details.get("focus", {})
        if isinstance(focus_dict, dict):
            for word, advice in focus_dict.items():
                if word in text:
                    results[level].append({"word": word, "advice": advice})
                    # 简单的重写逻辑：将关键词替换为建议中的核心词
                    # 这里提取建议中引号内的内容
                    import re
                    match = re.search(r"“(.+?)”", advice)
                    if match:
                        refined_text = refined_text.replace(word, match.group(1))
    
    return results, refined_text

def generate_markdown_report(original, refined, results, filename="Audit_Report.md"):
    with open(filename, "w", encoding="utf-8") as f:
        f.write(f"# 规则甄查 · 文案审计报告\n\n")
    

---
*从 Gemini 导出，2026-05*
