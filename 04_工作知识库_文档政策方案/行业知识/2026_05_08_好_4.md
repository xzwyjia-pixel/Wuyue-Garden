{"content":"---\ntitle: 好\nsource: gemini\ndate: 2026-05-08\ncategory: 技术开发\nsubcategory: Python\nold_category: 编程开发\ntags: [gemini, 技术开发, Python]\n---\n\n## 好\n\n## 好\n\n既然你已经准备好了，那我们就直接进入实战阶段。\n为了触发 Claude 的最高效开发模式（Superpowers），建议你按照下面的顺序，一个一个模块地要求它输出。\n第一步：开启核心——注入引擎 (engine.py)\n这是软件的“手”，负责物理修改 Windows 的设定。\n向 Claude 发送这个 Prompt：\n\n现在开始开发“语义文件夹图标助手”的第一部分：核心注入引擎。请创建一个 engine.py 模块，包含以下功能：\n\n权限处理：通过 ctypes 检查并申请管理员权限。\n核心函数 set_folder_customization(folder_path, icon_path, tip_text)：\n\n设置文件夹为“只读”属性（FILE_ATTRIBUTE_READONLY），这是 desktop.ini 生效的前提。\n在目标文件夹下生成隐藏、系统的 desktop.ini 文件，编码需符合 Windows 要求（UTF-16 LE）。\n写入 [.ShellClassInfo] 配置，指定 IconResource 和 InfoTip。\n\n\n刷新机制：调用 SHChangeNotify 确保图标修改后立即生效，无需重启资源管理器。\n复原功能：提供一个重置文件夹回默认状态的函数。\n\n\n\n第二步：构建大脑——语义分析器 (analyzer.py)\n这是软件的“眼睛”，负责看文件夹里有什么。\n等第一个模块完成后，发送这个 Prompt：\n\n接下来开发 analyzer.py 模块。这个模块需要通过分析文件夹特征输出对应的标签：\n\n静态规则提取：扫描文件夹内的扩展名分布。\n\n如果是 .py, .js, .git -> 标签为 Code\n如果是 .mp4, .mkv -> 标签为 Video\n如果是 .docx, .pdf, .xlsx -> 标签为 Office\n\n\n语义关键词匹配：分析文件夹名称。\n\n包含“学习”、“课程”、“Study” -> 标签为 Education\n包含“秘密”、“Private”、“Secret” -> 标签为 Lock\n\n\n智能权重系统：给出一个得分机制，如果某类文件占比超过 60%，直接判定该分类。请确保逻辑可扩展，方便以后接入 LLM。\n\n\n\n第三步：连接触角——实时监控器 (watcher.py)\n这是软件的“耳朵”，负责监听系统变动。\n发送这个 Prompt：\n\n请使用 watchdog 库开发 watcher.py：\n\n监控逻辑：监听指定根目录下“新建文件夹”或“重命名文件夹”的事件。\n异步缓冲：因为文件写入有延迟，当检测到新文件夹时，等待 2 秒（防抖）再触发 analyzer 进行扫描。\n配置读取：从一个简单的 JSON 文件中读取“标签-图标路径”的对应关系。\n\n\n\n💡 调试与进阶建议\n\n利用 Artifacts：Claude 在生成这些代码时，右侧通常会出现一个代码窗口。你可以点击代码窗口右上角的“Copy”，直接粘贴到 VS Code 中。\n报错处理：如果运行代码时出现 Access Denied 或图标不刷新，直接把报错信息发给 Claude。你可以说：“注入逻辑在普通文件夹生效，但在系统盘失败了，请优化 UAC 提权逻辑。”\n图标包：你可以让 Claude 写一个简单的 Python 脚本，去 Flaticon 或 Iconfont 的 API（如果有）批量下载对应标签的图标。\n\n你现在可以先在 Claude 里输入“第一步”的 Prompt，看看它给出的核心注入引擎质量如何！\n\n---\n*从 Gemini 导出，2026-05*\n\n---\n*Gemini · 技术开发 / Python · 2026-05-08*\n"}